# Leave empty to use relative /api paths (dev server proxy, or same-origin deploy).
# Set to the backend's URL to point at a specific server, e.g.:
# VITE_API_BASE_URL=http://89.117.59.12:4000
VITE_API_BASE_URL=
